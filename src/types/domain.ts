export type Domain = {
  domain: string;
  name: string;
  tld: string;
  length: number;
  has_numbers: boolean;
  is_number_only: boolean;
  has_hyphen: boolean;
  registrar: string;
  created_at: string;
};

